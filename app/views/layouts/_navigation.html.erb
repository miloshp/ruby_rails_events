<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans:300" type="text/css" />

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Momentum", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if logged_in? %>
          <br>
          <big>
            Logged in as <%= current_user.email %>
            Username : <%=  current_user.username%>
          </big>
          <li><%= link_to "Events", user_path(current_user) %></li>
          <li><%= link_to "New  Event", new_event_path %></li>
          <li><%= link_to "Log out", logout_path, method: :delete %></li>
        <% else %>
          <br>
          <li><%= link_to "Sign Up", signup_path, method: :get %></li>
          <li><%= link_to "Log In", login_path, method: :get %></li>
          <li><%= link_to "About", about_path %></li>
        <% end %>
          </ul>
        </li>
      </ul>
      <% if logged_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">SORT <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="#"><%= link_to "Name", sort_name_path %></a></li>
              <li><a href="#"><%= link_to "Upcoming", sort_due_path %></a></li>
              <li><a href="#"><%= link_to "Recent", sort_created_path %></a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">User : <%=  current_user.username%></a></li>
            </ul>
          </li>
        </ul>
      <%end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
